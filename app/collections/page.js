export const metadata = { title: 'Collections — South Grant' }
import StaticPage from '../components/StaticPage'
export default function Collections() { return <StaticPage eyebrow="South Grant / Explore" title="Collections" copy="Curated rooms for a more considered home." /> }
